# Excel Query Scheduler - dependencies
# Python 3.10+ (built on 3.13)
# Install:  pip install -r requirements.txt
#
# NOTE: pywin32 works only on Windows (Excel COM automation).

# --- GUI ---
PyQt6>=6.7.0

# --- Excel via COM (Windows only) ---
pywin32>=306

# --- Task scheduler ---
APScheduler>=3.10.4

# --- Timezone data (zoneinfo on Windows has no system IANA database) ---
tzlocal>=4.0
tzdata>=2024.1

# --- Build to .exe (needed only for release, Stage 7) ---
pyinstaller>=6.0.0

# Storage (SQLite, json) and system tray (QSystemTrayIcon) use only
# the standard library / PyQt6 — no extra packages needed.
# Analytics server (FastAPI, Stage 6) is a SEPARATE component with its
# own server/requirements.txt.